import { createTheme } from '@mui/material';

export const x = '';

export const theme = createTheme({
  palette: {
    background: {
      default: '#0a1929',
    },
    primary: {
      main: '#ffffff',
    },
    error: {
      main: '#ff0000',
    },
  },
});
